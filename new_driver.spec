# -*- mode: python -*-

block_cipher = None


a = Analysis(['new_driver.py'],
             pathex=['C:\\Users\\mariu\\Documents\\GitHub\\TSheets Helper'],
             binaries=[],
             datas=[('info.json', '.'), ('assets/*.json', 'assets/')],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          [],
          exclude_binaries=True,
          name='new_driver',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               name='new_driver')
